function printLetters(inputString) {
    for (var index = 0; index < inputString.length; index++) {
        var element = inputString[index];
        console.log(`str[${index}] -> ${element}`)
    }
}